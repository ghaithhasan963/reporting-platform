services:
  - type: web
    name: reporting-platform
    env: python
    plan: free
    buildCommand: pip install -r reporting_platform/requirements.txt
    startCommand: python reporting_platform/app.py
